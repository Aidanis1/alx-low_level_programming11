#include <stdio.h>
/**
 * main -print all single digit numbers of base 10
 * return: Always 0 (success)
 */

int main(void)
{
	int n = 0;
	
	while (n < 10)
	{
		printf("%d",n);
		n++;
	}
	return (0);
}
